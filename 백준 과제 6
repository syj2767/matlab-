#include <stdio.h>

int main(void) 
{
	int a,c;
	int max = -1000000;
	int min = 1000000;

	scanf("%d", &a);
	for (int i = 0;i < a ; i++)
	{
		scanf("%d", &c);
		if (c > max)
			max = c;
		if (c < min)
			min = c;
	}
	printf("%d %d", min, max);
	return 0;
}
