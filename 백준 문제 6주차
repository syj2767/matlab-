#include <stdio.h>

int main(void) 
{
	int a,c;
	int arr[10000];

	scanf("%d %d", &a,&c);
	for (int i = 0;i < a ; i++)
	{
		scanf("%d", &arr[i]);
	}
	for (int i = 0;i < a;i++)
	{
		if (arr[i] < c)
		{
			printf("%d", arr[i]);
			if (i != a - 1)
				printf(" ");
		}

	}
	return 0;
}
