#include <stdio.h>

int main(void) 
{
	int a = 9;
	int c;
	int maxcount = 1;
	int max = 0;
	

	for (int i = 1;i <= a ; i++)
	{
		scanf("%d", &c);
		if (c > max)
		{
			max = c;
			maxcount = i;
			
		}
	}
	printf("%d %d", max, maxcount);
	return 0;
}
