#include <stdio.h>

int main()
{
    int test;
    int a, b;
    scanf("%d", &test);
    for (int i = 1; i < test+1; i++)
    {
        scanf("%d %d", &a, &b);
        printf("Case #%d: %d\n",i,a + b);
    }
    return 0;
}
